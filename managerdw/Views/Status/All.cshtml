@model IEnumerable<managerdw.Models.Status>
@{
    ViewBag.Title = "Все добавленные статусы";
    Layout = "~/Views/Shared/_PanelControl.cshtml";
}


<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">


                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Название статуса</th>
                                <th>Удаление</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var status in Model)
                            {
                            <tr>
                                @if (status.Name == "Просрочен")
                                {
                                    <td><span class="label label-danger">@status.Name</span></td>
                                }
                                else if (status.Name == "Приостановлен")
                                {
                                    <td><span class="label  label-warning">@status.Name</span></td>
                                }
                                else if (status.Name == "В работе")
                                {
                                    <td><span class="label  label-info">@status.Name</span></td>
                                }
                                else if (status.Name == "Выполнен") {
                                
                                  <td><span class="label label-success">@status.Name</span></td>

                                }
                                else {                                
                                  <td><span class="label label-inverse">@status.Name</span></td>
                                }
                                <td>@Html.ActionLink("Удалить", "Delete", "Status", new { id = status.Id }, new { @class = "btn btn-danger  btn-sm" })</td>
                            </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>




